{{#each posts as |post| }}
<div>
    <div>
        <h2>
            <a href="/post/{{post.id}}">{{post.title}}</a>
            <p>Posted by {{post.username}} on {{ format_date post.date_created}}</p>
        </h2>
    </div>
    <div>
        <p>{{post.text}}</p>
    </div>
</div>
{{/each}}